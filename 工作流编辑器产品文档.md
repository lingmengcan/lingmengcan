# 工作流编辑器产品文档

## 产品概述

工作流编辑器是一个可视化的流程设计工具，允许用户通过拖拽和连接的方式创建复杂的AI工作流。该编辑器提供了直观的界面，支持多种节点类型，并能够实时预览和测试工作流的执行效果。

## 核心功能

### 1. 可视化流程设计
- **节点式编辑**：采用节点和连线的方式构建工作流
- **拖拽操作**：支持节点的自由拖拽和位置调整
- **连线管理**：通过连线定义节点间的数据流向和执行顺序

### 2. 节点类型

#### 开始节点
- **功能**：工作流的入口点
- **输入**：接收外部输入参数
- **标识**：蓝色圆形图标，标注"开始"

#### 大模型节点
- **功能**：集成AI大语言模型进行文本处理
- **配置项**：
  - 输入：`input` - 接收上游数据
  - 输出：`output` - 处理后的结果，`reasoning_content` - 推理内容
  - 模型：DeepSeek-R1/250528
  - 技能：支持自定义技能配置

#### 结束节点
- **功能**：工作流的出口点
- **输出**：返回最终结果给调用方
- **标识**：紫色圆形图标，标注"结束"

### 3. 编辑器界面

#### 工具栏功能
- **缩放控制**：100% 缩放比例调节
- **视图操作**：
  - 适应画布大小
  - 居中显示
  - 全屏模式
- **添加节点**：快速添加新的工作流节点
- **运行测试**：一键测试工作流执行效果

#### 画布操作
- **无限画布**：支持大型复杂工作流的设计
- **网格背景**：提供对齐参考
- **多选操作**：支持批量选择和操作节点
- **撤销重做**：支持操作历史管理

### 4. 数据流管理

#### 参数传递
- **输入映射**：定义节点间的数据传递关系
- **输出配置**：设置节点的输出参数
- **类型检查**：确保数据类型的兼容性

#### 执行流程
- **顺序执行**：按照连线顺序执行节点
- **并行处理**：支持多分支并行执行
- **条件分支**：基于条件进行流程控制

## 技术架构

### 前端技术栈
- **Vue 3**：现代化的前端框架
- **TypeScript**：类型安全的开发体验
- **Pinia**：状态管理
- **Element Plus**：UI组件库

### 核心组件
- **WorkflowDesigner**：主编辑器组件
- **NodeRenderer**：节点渲染器
- **ConnectionManager**：连线管理器
- **ToolbarComponent**：工具栏组件

### 数据结构
```typescript
interface WorkflowNode {
  id: string;
  type: 'start' | 'llm' | 'end';
  position: { x: number; y: number };
  data: {
    label: string;
    inputs: Record<string, any>;
    outputs: Record<string, any>;
  };
}

interface WorkflowConnection {
  id: string;
  source: string;
  target: string;
  sourceHandle: string;
  targetHandle: string;
}
```

## 使用指南

### 创建工作流
1. **添加开始节点**：设置工作流的输入参数
2. **添加处理节点**：根据需求添加大模型或其他处理节点
3. **配置节点参数**：设置每个节点的具体参数
4. **连接节点**：通过拖拽建立节点间的连接
5. **添加结束节点**：定义工作流的输出格式

### 节点配置
1. **选择节点**：点击节点进入配置模式
2. **设置参数**：在右侧面板配置节点参数
3. **映射输入**：定义输入数据的来源
4. **配置输出**：设置输出数据的格式

### 测试运行
1. **点击运行按钮**：启动工作流测试
2. **输入测试数据**：在开始节点提供测试输入
3. **查看执行结果**：观察每个节点的执行状态和输出
4. **调试优化**：根据结果调整节点配置

## 最佳实践

### 设计原则
- **模块化设计**：将复杂逻辑拆分为多个简单节点
- **清晰命名**：为节点和参数使用有意义的名称
- **错误处理**：在关键节点添加错误处理逻辑
- **性能优化**：避免不必要的数据传递和计算

### 常见模式
- **线性流程**：开始 → 处理 → 结束
- **分支合并**：一个输入，多个处理分支，最后合并
- **循环处理**：通过条件节点实现循环逻辑
- **并行处理**：多个独立的处理分支

## 扩展功能

### 自定义节点
- 支持开发自定义节点类型
- 提供节点开发SDK
- 支持第三方插件集成

### 模板库
- 预置常用工作流模板
- 支持模板导入导出
- 社区模板分享

### 版本管理
- 工作流版本控制
- 变更历史追踪
- 回滚功能

## 技术支持

### API接口
- 工作流CRUD操作
- 节点类型管理
- 执行引擎接口

### 监控告警
- 执行状态监控
- 性能指标统计
- 异常告警机制

### 部署运维
- 容器化部署
- 水平扩展支持
- 高可用架构

## 更新日志

### v1.0.0
- 基础工作流编辑功能
- 支持开始、大模型、结束节点
- 可视化连线操作
- 基础测试运行功能

---

*本文档持续更新中，如有问题请联系开发团队*